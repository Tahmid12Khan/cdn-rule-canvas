/// <reference types="vitest/globals" />
/// <reference types="@testing-library/jest-dom" />

// Enables Vitest global APIs (describe/it/expect/vi) and jest-dom matchers in
// test files without per-file imports, since vitest.config.ts sets
// `globals: true`.
